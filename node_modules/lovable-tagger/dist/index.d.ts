import { Plugin } from 'vite';

interface TaggerOptions {
    jsxSource?: boolean;
    tailwindConfig?: boolean;
    virtualOverrides?: boolean;
    debug?: boolean;
}
declare function  Tagger({ jsxSource, tailwindConfig, virtualOverrides, debug, }?:  TaggerOptions): Plugin;

export { type  TaggerOptions,  Tagger as componentTagger };
